<h2>Ändra djur/djurgrupp</h2>
<% if @animal_group.errors.any? %>
<div id="error_explanation">
  <h2><%= @animal_group.errors.count %> fel</h2>
  <ul>
    <% @animal_group.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
    <% end %>
  </ul>
</div>
<% end %>
<%= form_with(model: @animal_group, local: true) do |form| %>
<div class="form-group">
  <label>Namn</label>
  <%= form.text_field :name, placeholder: @animal_group.name %>
</div>
<div class="form-group">
  <label>Antal</label>
  <%= form.text_field :amount, placeholder: @animal_group.amount %>
</div>
<%= form.submit 'Spara', {class: 'btn btn-success'} %> <%= link_to '<button class="btn btn-info">Visa</button>'.html_safe, @animal_group %> <%= link_to '<button class="btn btn-secondary">Tillbaka</button>'.html_safe, animal_groups_path %>
<% end %>
