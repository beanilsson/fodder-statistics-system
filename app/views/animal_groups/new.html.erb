<h2>Nytt djur/ny djurgrupp</h2>
<% if @animal_group.errors.any? %>
<div id="error_explanation">
  <h2><%= @animal_group.errors.count %> fel</h2>
  <ul>
    <% @animal_group.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
    <% end %>
  </ul>
</div>
<% end %>
<%= form_with scope: :animal_group, url: animal_groups_path, local: true do |form| %>
<div class="form-group">
  <label for="batchName">Namn</label>
  <%= form.text_field :name %>
</div>
<div class="form-group">
  <label for="batchAmount">Mängd</label>
  <%= form.number_field :amount %>
</div>
<%= form.submit 'Spara', {class: 'btn btn-success'} %> <%= link_to '<button class="btn btn-secondary">Tillbaka</button>'.html_safe, animal_groups_path %>
<% end %>
